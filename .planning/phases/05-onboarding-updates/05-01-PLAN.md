# Plan 05-01: Update onboarding for new tools

## Goal

Update the onboarding wizard to guide users through installing and configuring the three new developer productivity tools (claude-yolo, claude-notify, get-shit-done) that were added in phases 2-4.

## Context

- **Phase**: 5 of 5 - Onboarding updates (final phase of v1.1 milestone)
- **Depends on**: Phases 2-4 must be complete (skills created)
- **Pattern**: Follow existing onboarding patterns in CLAUDE.md and onboard skill

## Tasks

- [x] Task 1: Update CLAUDE.md step list with steps 14-16
- [x] Task 2: Update CLAUDE.md verification commands
- [x] Task 3: Update CLAUDE.md discovery commands
- [x] Task 4: Update config.example.json optional_tools
- [x] Task 5: Update onboard skill available skills table
- [x] Task 6: Update onboard skill Phase 3 section
- [ ] Task 7: Commit changes

## Files to Create/Modify

| File | Action | Description |
|------|--------|-------------|
| `CLAUDE.md` | Modify | Add step entries (14-16), verification commands, discovery commands |
| `config.example.json` | Modify | Add claude_yolo, claude_notify, get_shit_done toggles |
| `.claude/skills/onboard/SKILL.md` | Modify | Add to skills table and Phase 3 optional tools |

## Detailed Changes

### CLAUDE.md Step List (around line 79)

Add:
```markdown
14. `steps/14-claude-yolo.md` - YOLO mode wrapper for unattended execution **[QUICK]**
15. `steps/15-claude-notify.md` - Desktop notifications for long tasks **[RECOMMENDED]**
16. `steps/16-get-shit-done.md` - Meta-prompting system for structured projects **[OPTIONAL]**
```

### CLAUDE.md Verification Commands (around line 134)

Add:
```bash
# claude-yolo
which cl && cl /STATUS

# claude-notify
which cn && cn status

# get-shit-done
# Verify by running /gsd:help in Claude Code
```

### CLAUDE.md Discovery Commands (around line 48)

Add:
```bash
which cl && echo "claude-yolo: installed" || echo "claude-yolo: not installed"
which cn && echo "claude-notify: installed" || echo "claude-notify: not installed"
```

### config.example.json

Add to `setup.optional_tools`:
```json
"claude_yolo": true,
"claude_notify": true,
"get_shit_done": false
```

### onboard skill Available Skills table

Add:
```markdown
| `/setup-claude-yolo` | Installing YOLO mode wrapper |
| `/setup-claude-notify` | Setting up desktop notifications |
| `/setup-gsd` | Installing meta-prompting system |
```

### onboard skill Phase 3 section

Add after existing tools:
```markdown
#### claude-yolo (`optional_tools.claude_yolo`)
**Use the `/setup-claude-yolo` skill** - YOLO mode wrapper for unattended execution.

#### claude-notify (`optional_tools.claude_notify`)
**Use the `/setup-claude-notify` skill** - desktop notifications for long tasks.

#### Get Shit Done (`optional_tools.get_shit_done`)
**Use the `/setup-gsd` skill** - meta-prompting for structured projects.
```

## Dependencies

**Must exist before this plan executes** (created in phases 2-4):
- `.claude/skills/setup-claude-yolo/SKILL.md`
- `.claude/skills/setup-claude-notify/SKILL.md`
- `.claude/skills/setup-gsd/SKILL.md`

## Verification

- [ ] `grep -n "14-claude-yolo" CLAUDE.md` shows step entry
- [ ] `cat config.example.json | jq '.setup.optional_tools'` shows new toggles
- [ ] `grep -n "setup-claude-yolo" .claude/skills/onboard/SKILL.md` shows skill reference
- [ ] Manual test: Ask Claude "help me get started" and verify new tools appear

## Success Criteria

- [ ] CLAUDE.md lists steps 14, 15, 16 with appropriate tags
- [ ] CLAUDE.md verification section includes new tool checks
- [ ] CLAUDE.md discovery section checks for new tools
- [ ] config.example.json has toggles for all three tools
- [ ] Onboard skill references new skills
- [ ] Changes committed

---
*Plan created: 2026-01-15*
